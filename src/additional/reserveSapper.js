'use strict'


// ! variant check neighbourth bomb for sphere sapper
/*
function isEmptyField(row, column) {
	const index = row * WIDTH + column;
	let indexMainArray;
	let cell;
	for (let x = -1; x <= 1; x++) {
		for (let y = -1; y <= 1; y++) {
			indexMainArray = (row + y) * WIDTH + (column + x);
			cell = cells[indexMainArray];
		}
	}
}
isEmptyField(row, column)
*/